version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sq-product-admin-server
    ports:
      - "4402:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - MONGODB_URI=mongodb+srv://jahirulislamshozib0_db_user:i5Kc4we2CrLTAYIO@cluster0.saoc7bo.mongodb.net/sq_ecom_DB?retryWrites=true&w=majority&appName=Cluster0
      - CORS_ORIGINS=http://localhost:4200,http://172.16.234.38:4402,http://172.16.234.38:4200
      - EMAIL_API_URL=http://172.16.234.51:3335
      - JWT_SECRET=your_super_secret_key_change_this
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
